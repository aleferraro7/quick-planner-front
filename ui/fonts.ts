import { Lusitana, Montserrat, PT_Sans } from 'next/font/google';

export const pt_sans = PT_Sans({ subsets: ['latin'], weight: '700' });

export const montserrat = Montserrat({ subsets: ['latin'] });

export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
